package com.example.match.repository;

import com.example.match.entities.Stade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StadeRespository extends JpaRepository<Stade,Long> {
}
